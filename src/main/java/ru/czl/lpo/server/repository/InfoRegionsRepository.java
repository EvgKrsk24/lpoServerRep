package ru.czl.lpo.server.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import ru.czl.lpo.server.entity.InfoRegions;

public interface InfoRegionsRepository extends JpaRepository<InfoRegions, Integer> {
}
